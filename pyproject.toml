[tool.ruff]
line-length = 100

[tool.ruff.lint]
per-file-ignores = { "portfolio_tracker_api/settings/*.py" = ["F403", "F405"] }

[tool.black]
line-length = 100
target-version = ["py313"]

[tool.isort]
profile = "black"
line_length = 100
